using UnityEngine;

// WhiteSpace(공백) : 공백은 프로그램에서 무시

public class WhiteSpace : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("c#");
        Debug.  Log  (  "c#"  );
        Debug.
            Log(
            "c#");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
