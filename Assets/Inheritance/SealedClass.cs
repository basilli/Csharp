using UnityEngine;

/* [0] 개요 : SealedClass (봉인클래스)
*/

namespace Inheritance07
{
    public class SealedClass : MonoBehaviour
    {
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {
            // [1] 결과 : CatClass의 인스턴스 생성.

            // ) Cat cat = new Cat();
            // ) cat.Eat();
        }
    }
}
